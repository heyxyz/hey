deploy-og:
  only:
    - main
  image: docker:27.2.0
  stage: deploy
  before_script:
    - apk add --no-cache curl
  environment: production
  services:
    - docker:27.2.0-dind
  variables:
    GITLAB_IMAGE_TAG: $CI_REGISTRY/yo/hey/og:latest
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login $CI_REGISTRY -u $CI_REGISTRY_USER --password-stdin
    - docker build -t $GITLAB_IMAGE_TAG -f ./apps/og/Dockerfile .
    - docker push $GITLAB_IMAGE_TAG
    - curl -s "https://redeploy.heyxyz.workers.dev?secret=$SECRET&service=$OG_RAILWAY_SERVICE_ID"
