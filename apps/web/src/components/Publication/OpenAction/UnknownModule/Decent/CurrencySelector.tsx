import type { FC } from 'react';

interface CurrencySelectorProps {}

const CurrencySelector: FC<CurrencySelectorProps> = ({}) => {
  return <div className="h-[80vh] p-5">Select currency</div>;
};

export default CurrencySelector;
