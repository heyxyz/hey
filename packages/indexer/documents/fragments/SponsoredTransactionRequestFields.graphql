fragment SponsoredTransactionRequestFields on SponsoredTransactionRequest {
  reason
  raw {
    chainId
    data
    from
    gasLimit
    nonce
    to
    type
    value
    customData {
      customSignature
      factoryDeps
      gasPerPubdata
      paymasterParams {
        paymaster
        paymasterInput
      }
    }
  }
}
