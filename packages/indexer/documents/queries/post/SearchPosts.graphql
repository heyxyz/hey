query SearchPosts($request: SearchPostsRequest!) {
  searchPosts(request: $request) {
    items {
      ... on Post {
        ...PostFields
      }
      ... on Repost {
        ...RepostFields
      }
    }
    pageInfo {
      ...PaginatedResultInfoFields
    }
  }
}
