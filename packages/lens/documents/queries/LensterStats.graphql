query LensterStats {
  globalProtocolStats(request: { sources: "Lenster" }) {
    totalProfiles
    totalPosts
    totalBurntProfiles
    totalMirrors
    totalComments
    totalCollects
    totalFollows
  }
}
